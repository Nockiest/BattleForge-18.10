extends Node


 
# Called every frame. 'delta' is the elapsed time since the previous frame.
#func process(state) -> void:
#	if state == owner.state.Moving:
#		$random_music_player.active = true
#	else:
#		$random_music_player.active = false
